let { $, sleep } = require('./funcs');

module.exports = function () {

  //Scenario: When I have signed in I would lik to add rating on objects
  this.Then(/^Click add to watchlist on the first move on page$/, function () {
  });
  this.Then(/^save that movie name$/, function () {
  });
  this.Then(/^browse to my watchlist$/, function () {
  });
  this.Then(/^the same movie should be present$/, function () {
  });
}