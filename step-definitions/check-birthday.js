let { $, sleep } = require('./funcs');

module.exports = function () {

  //Scenario: Cross-check our date of birth with the celebertys everipedia pages
  this.When(/^I browse to Birth Month Day of todays$/, function () {
  });
  this.Then(/^the first (\d+) names should be saved to a list$/, function (value5) {
  });
  this.Then(/^if the list contains white space replace that with _$/, function () {
  });
  this.Then(/^add "([^"]*)" before the name$/, function (everipediaUrl) {
  });
  this.Then(/^browse to that page$/, function () {
  });
  this.Then(/^find birthday on that page and check if it is today$/, function () {
  });

}